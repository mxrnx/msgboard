<% threads.forEach(thread => { %>
    <div class="thread">
        <%- include('partials/originalPost.ejs', { thread }) %>

        <% thread.replies.forEach(reply => { %>
            <%- include('partials/reply.ejs', { reply }) %>
        <% }) %>
    </div>
<% }) %>

<h2>colloquium novum</h2>
<form method="POST" action="/addPost" class="new-thread-form">
    <div class="form-row">
        <div class="form-group">
            <label for="icon">Sigillum</label>
            <select name="icon" id="icon" required>
                <% threadIcons.forEach(icon => { %>
                    <option><%= icon %></option>
                <% }) %>
            </select>
        </div>
        <div class="form-group grow">
            <label for="title">De qua re loqui vis?</label>
            <div class="input-title">
                <span style="">De</span>
                <input type="text" placeholder="libris praeferendis" name="title" id="title" required />
            </div>
        </div>
    </div>

    <div class="form-group">
        <label for="message">Nuntium</label>
        <textarea name="message" id="message" rows="6" required></textarea>
    </div>

    <button type="submit">Mittere</button>
</form>
